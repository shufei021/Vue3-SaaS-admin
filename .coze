[project]
requires = ["nodejs-24"]

[dev]
build = ["pnpm", "install"]
run = ["pnpm", "run", "dev"]

[deploy]
build = ["bash", "-c", "pnpm install && pnpm run build"]
run = ["sh", "-c", "npx vite preview --port ${DEPLOY_RUN_PORT} --host 0.0.0.0"]
